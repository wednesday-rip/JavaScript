console.log("JavaScript is running!!!")

//don't use (var) use (let) for creating variables.
let nickName = "Wednesday"
console.log(nickName)

//To create a variable that cannot be changed, use (const) not (let).
const price =30
console.log(price)

let itemName = "Pen" // string
let itemPrice =  3 // number
let itemAvailable =  true // true and false are boolean
let itemColor  // When the variable is not defined, it is an undefined status.
itemName = null // should use (null) when you want to clear the content stored in the variable

